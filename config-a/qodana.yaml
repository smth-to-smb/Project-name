version: "1.0"

imports:
  - ../base/qodana.yaml  # path to base profile to use
  
failureConditions:
  severityThresholds:
    any: 0

profile:
    name: qodana.recommended

inspections:
    - group: "category:Java/Probable bugs"
      enabled: true
